class LoginModel:
    def __int__(self, id, name, login, email, password):
        self.id = id,
        self.name = name,
        self.login = login,
        self.email = email,
        self.password = password